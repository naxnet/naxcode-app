<%= form_with(model: [:professors, course], local: true) do |form| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
        <% course.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: "form-control", placeholder: "Name of course" %>
  </div>

  <div class="actions">
    <%= link_to 'Back', professors_courses_path, class: "btn btn-light" %>
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
